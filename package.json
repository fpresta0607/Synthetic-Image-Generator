{
  "name": "synthetic0-root",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "server"
  ],
  "scripts": {
    "dev": "npm run dev -w server",
    "dev:gpu": "npm run dev:gpu -w server",
    "dev:win": "npm run dev:win -w server",
    "start": "npm run start -w server",
    "test": "npm run test -w server"
  }
}
