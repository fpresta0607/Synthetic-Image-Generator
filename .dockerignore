# General
.git
.gitignore
README.md
.env*
py/.venv
**/__pycache__
**/*.pyc
node_modules
server/node_modules
npm-debug.log
yarn.lock
*.zip
.tmp
.DS_Store

# Model artifacts (previously excluded to keep images slim)
# We are now baking the SAM checkpoint directly into the image, so allow models directory.
# models/
# py/models/

# Temp / runtime outputs
tmp_datasets/
tmp/
outputs/
__outputs__/
dist/

# Large image/testing folders (add if present)
data/
datasets/
samples/
